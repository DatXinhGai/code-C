//
// Created by HP LAPTOP on 02/03/2025.
//
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
// gọi nhiều lần nếu 0 thì return ;
typedef struct {
    int character;
    int dem;
};

int hoanViXau_re(int* dem, int dai, ) {

}


int hoanViXau() {
    char* temp = (char*)malloc(1024);
    fgets(temp, 1023, stdin);
    temp[strcspn(temp, "\r\n")] = '\0';
    int dai = strlen(temp);
    int* dem = (int*)calloc(dai, sizeof(int));
    while (*temp) {
        dem[*temp]++;
        temp++;
    }
}
*/